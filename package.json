{
  "name": "dstris",
  "version": "0.1.0",
  "description": "Competitive single-player Tetris vs CPU",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
